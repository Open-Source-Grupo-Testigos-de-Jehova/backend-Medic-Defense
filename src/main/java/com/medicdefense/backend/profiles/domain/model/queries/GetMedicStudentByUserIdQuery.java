package com.medicdefense.backend.profiles.domain.model.queries;

import com.medicdefense.backend.profiles.domain.model.valueobjects.UserId;

public record GetMedicStudentByUserIdQuery(UserId userId) {
}
